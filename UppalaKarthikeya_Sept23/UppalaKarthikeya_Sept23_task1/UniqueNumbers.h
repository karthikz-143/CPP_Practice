#ifndef UNIQUENUMBERS_H
#define UNIQUENUMBERS_H

// To check if a number is already present in the array
bool isDuplicate(int arr[], int size, int number);

// To display unique numbers
void displayUnique(int arr[], int size);

#endif
